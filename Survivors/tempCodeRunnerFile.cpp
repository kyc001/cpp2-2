#include<bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);cout.tie(0);

    char s1[] = "this is a Book", s2[] = "this is a Haik";
    int i = 0;
    while(s1[i] = s2[i], i ++);
    printf("%s\n%s", s1, s2);

    return 0;
}